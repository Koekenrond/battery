let getal_down = 0
let getal_up = 0
let getal = 0
let A = 1
let B = 0
basic.forever(function () {
    getal = A / B
    getal_up = Math.ceil(getal)
    getal_down = Math.floor(getal)
    if (getal == getal_up || getal == getal_down) {
        if (true) {
        	
        }
    }
})
